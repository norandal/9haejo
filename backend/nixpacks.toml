[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "PYTHONUTF8=1 PYTHONIOENCODING=utf-8 uvicorn main:app --host 0.0.0.0 --port $PORT"
